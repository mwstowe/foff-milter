name: "Test Module"
enabled: true
rules:
  - name: "Test Rule"
    enabled: true
    criteria:
      type: "PhishingSuspiciousLinks"
      check_url_shorteners: true
      unsupported_field: "this should cause an error"
    score: 10
    action:
      type: "TagAsSpam"
      header_name: "X-Test"
      header_value: "Test"
