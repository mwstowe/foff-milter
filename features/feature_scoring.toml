[feature_scoring]

[feature_scoring.link_analysis]
enabled = true
base_score = 0
confidence_threshold = 0.5
suspicious_patterns = []

[feature_scoring.sender_alignment]
enabled = true
base_score = 0
confidence_threshold = 0.5
job_related_exclusion = true
job_indicators = []

[feature_scoring.context_analysis]
enabled = true
base_score = 0
confidence_threshold = 0.5
urgency_patterns = []

[feature_scoring.invoice_analysis]
enabled = true
base_score = 0
confidence_threshold = 0.5
scam_indicators = [
    "(overdue|total amount|invoice number)",
    "(within 24 hours|payment required|account suspended)",
    "\\b(click\\s+here|verify\\s+.*\\s+account|update\\s+.*\\s+payment)\\b",
    "(invoice\\s+and\\s+details\\s+enclosed)",
    "(thank\\s+you\\s+for\\s+paying\\s+for\\s+order)",
    "(thank\\s+you\\s+for\\s+choosing\\s+us.*invoice)",
    "(dear.*payment.*received.*invoice.*attached)",
    "(dear,.*payment.*received.*invoice.*attached)",
    "(order.*successfully\\s+processed)",
    "(find\\s+attached.*order\\s+summary)",
    "(greetings.*order.*processed)"
]

[spam_thresholds]
accept = 0
tag_as_spam = 50
reject = 350

[rule_scoring]
