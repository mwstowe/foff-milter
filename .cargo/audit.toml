# cargo-audit configuration

[advisories]
# ttf-parser is unmaintained but pulled in transitively via lopdf → pdf-extract.
# No replacement available yet; acknowledge the warning.
ignore = ["RUSTSEC-2026-0192"]
