# Legacy YAML Rules (Optional)
# Used only when [legacy] enabled = true in main TOML config

socket_path: "/var/run/foff-milter.sock"
rules:
  - name: "Legacy spam pattern"
    criteria:
      type: "SubjectPattern"
      pattern: "(?i).*(viagra|cialis).*"
    action:
      type: "Reject"
      message: "Rejected by legacy rule"

default_action:
  type: "Accept"

statistics:
  enabled: false
