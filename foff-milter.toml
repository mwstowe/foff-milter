# FOFF Milter Testing Configuration
# Overrides defaults to use local development directories

[rulesets]
enabled = true
config_dir = "rulesets"

[features]
enabled = true
config_dir = "features"

[heuristics]
reject_threshold = 350
spam_threshold = 40    # Lowered to 40 to accept Poshmark at 67
accept_threshold = 0
