# FOFF Milter Production Configuration
# All settings use sane platform-specific defaults
# Only specify settings you want to override

# Uncomment to override defaults:
# [system]
# socket_path = "/var/run/foff-milter.sock"
# reject_to_tag = true

# [heuristics]
# reject_threshold = 350
# spam_threshold = 50
# accept_threshold = 0
