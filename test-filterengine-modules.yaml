socket_path: "/tmp/foff-milter.sock"
default_action:
  type: "Accept"
module_config_dir: "/tmp/test-modules"
rules:
  - name: "Force legacy path"
    criteria:
      type: "SenderPattern"
      pattern: "never-match@example.com"
    action:
      type: "Accept"
version: "0.5.0"
