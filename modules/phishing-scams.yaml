name: "Phishing Scams"
enabled: true
rules:
  - name: "Mailer daemon spoofing"
    criteria:
      type: "And"
      criteria:
        - type: "SenderPattern"
          pattern: ".*MAILER-DAEMON.*"
        - type: "HeaderPattern"
          pattern: ".*dkim=fail.*"
    action:
      type: "TagAsSpam"
      header_name: "X-Spam-Flag"
      header_value: "YES"
